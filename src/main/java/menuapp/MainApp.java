package menuapp;

/**
 * Entry point. Builds the model, the single AppController, and the GUI, then
 * shows the window. It injects the shared Menu, Inventory, and FileHandler into
 * the controller.
 */
public class MainApp {

  /**
   * Starts the application.
   *
   * @param args unused
   */
  public static void main(String[] args) {
    // Wired later during integration: seed menu and inventory, build
    // AppController and MainFrame, then show the window.
  }
}
